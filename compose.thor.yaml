services:
  runtime:
    build:
      context: .
      dockerfile: docker/runtime/Dockerfile
      args:
        BASE_IMAGE: ${THOR_RUNTIME_BASE:-nvidia/cuda:13.0.2-devel-ubuntu24.04}
        USER_ID: ${UID:-1000}
        GROUP_ID: ${GID:-1000}
        USERNAME: ${CONTAINER_USER:-chromie}
    image: soridormi-runtime:thor
    container_name: soridormi-runtime-thor
    stdin_open: true
    tty: true
    init: true
    network_mode: host
    ipc: host
    privileged: true
    runtime: nvidia
    environment:
      SORIDORMI_BACKEND: hardware
      CONTROL_HZ: ${CONTROL_HZ:-50}
      POLICY_PATH: /workspaces/Open_Duck_Mini/BEST_WALK_ONNX_2.onnx
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: all
      PYTHONUNBUFFERED: "1"
    volumes:
      - ./src:/app/src
      - ./pyproject.toml:/app/pyproject.toml:ro
      - ./workspace/Open_Duck_Mini:/workspaces/Open_Duck_Mini
      - ./workspace/Open_Duck_Mini_Runtime:/workspaces/Open_Duck_Mini_Runtime
      - ./data:/data
      - runtime-venv-thor:/opt/venvs/runtime
      - /dev:/host-dev
    working_dir: /app
    command: bash

volumes:
  runtime-venv-thor:
